# Add this inside the uam.khurk.xyz site block in /etc/caddy/Caddyfile
#
# Example site block:
#
#   uam.khurk.xyz {
#       # ... existing config ...
#
#       handle /api/rankd/* {
#           reverse_proxy localhost:3001
#       }
#   }

handle /api/rankd/* {
    reverse_proxy localhost:3001
}
