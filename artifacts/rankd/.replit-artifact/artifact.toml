kind = "web"
previewPath = "/"
title = "RANKD"
version = "1.0.0"
id = "artifacts/rankd"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22639

[services.development]
run = "pnpm --filter @workspace/rankd run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rankd", "run", "build" ]
publicDir = "artifacts/rankd/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22639"
BASE_PATH = "/"
