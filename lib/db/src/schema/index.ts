// Database schema — skeleton for future use.
// The API server currently uses in-memory caches; no DB tables are needed yet.
// When persistence is required (e.g. historical snapshots, user preferences),
// define tables here and run `pnpm --filter @workspace/db run push`.

export {}